# Synthetic PRD Snippet

Users can create routing rules for an account. The `rule_code` must be unique among active rules under the same account. If a user submits a duplicate active `rule_code`, the system must reject the request and show `Rule code already exists`.
