# Q01 需求结构化报告

## 结论

Q01 已完成需求结构化，抽取 4 条主需求、11 条业务规则、16 条关键语义。当前结论为 `PASS_WITH_GAPS`。

这份报告的读法是：先看 GAP/OPEN，因为它们代表需求或实现证据还没有闭环；再看 REQ/BR/SE，它们是后续 Q05a 设计 EUT、Q05b 写测试、Q06 审计覆盖的唯一语义基线。

## 怎么读这些编号

- `REQ-*` 是需求点，说明业务目标；`BR-*` 是业务规则，说明必须满足的分支规则；`SE-*` 是关键语义，说明必须能被验证的业务不变量。
- `EUT-*` 是 Executable Unit Test，即一条可落地到 Java 单测的业务场景，不等同于一个测试方法，但必须能追到测试方法。
- `T1/T2/T3` 是风险等级，不是技术优先级。T1 表示不测会影响状态、权限、审批、数据一致性、金额/Excel 或外部同步；T2 表示重要但爆炸半径较小。
- `B-*` 是代码路径编号，只用于追溯 Java 分支；报告中不能只展示 `B-*`，必须同时说明它对应的业务场景。
- `O-*` 是业务后果编号，只用于把代码路径绑定到可断言的业务结果；报告中不能只展示 `O-*`，必须说清楚不覆盖会造成什么后果。

## 本次最关键风险

| 风险 | 业务后果 | 需要谁决策/补证据 |
|---|---|---|
| GAP-001 P0：PRD 明确要求新版本生效后旧版本失效且 BI 只取生效版本，但当前已审代码未看到 INVALID/失效状态或 BI 同步过滤实现。 | 用户已明确要求本轮忽略，因此该风险不再阻断 Q06；作为已接受风险留痕。 | 后续若重新纳入范围，再明确旧版本失效模型和 BI 同步接口位置。 |
| GAP-002 P1：飞书图片/画板媒体下载权限不足，通知流程图只能基于浏览器截图和 PRD 文本解析。 | 如果不闭环，通知流程图语义可能依赖人工复核，影响通知链路设计准确性。 | 需要产品或文档 owner 提供可下载画板或确认浏览器解析的流程语义。 |

## 输入证据

| 类型 | 路径 | 说明 |
|---|---|---|
| PRD | `input/prd.md` | 飞书需求文档下载后的正文。 |
| PRD 评论 | `input/prd_comments.md` | 评论和补充说明。 |
| 飞书下载报告 | `input/feishu_download_report.md` | 下载过程和媒体可用性记录。 |
| 图片语义解析 | `q01/image_semantics.md` | 状态机/流程图的多模态解析结果。 |

## 需求与业务规则

| ID | 层级 | 说明 | 验收/后续验证重点 | 来源 |
|---|---|---|---|---|
| REQ-001 | 主需求 | 授权店财务手工上传指定科目并审批，最终计算门店经营模型。 | 每个未关闭授权店在目标月份有一条可提报任务；审批通过数据可进入经营模型。 | prd.md:119 |
| BR-001 | 业务规则，父级 REQ-001 | 每个月只针对未关闭门店生成门店+月份任务，已关闭门店不生成任务。 | 同一门店同一月份 V1 已存在时不得重复创建。 | prd.md:261 |
| BR-002 | 业务规则，父级 REQ-001 | 每月 1 日通知门店财务上传，每月 15 日提醒未提交门店的财务、城市经理和区域财经BP。 | 通知对象和文案包含门店名称、处理入口，并对已成功发送记录幂等过滤。 | prd.md:219 |
| BR-003 | 业务规则，父级 REQ-001 | 列表支持任务编号、版本、月份、审批流单号、门店、状态、创建时间和更新时间筛选，并按当前列表范围导出。 | 时间范围必须成对传入，导出结果受同一筛选条件和权限范围约束。 | prd.md:267 |
| REQ-002 | 主需求 | 任务表单以门店+月份为单位，支持填写、提交、BPM审批流和版本管理。 | 状态、版本、附件、审批流单号和版本记录与操作结果一致。 | prd.md:225 |
| BR-004 | 业务规则，父级 REQ-002 | 表单状态通过 BPM 审批关联控制状态机流转，归档之后不能人工编辑。 | 归档状态保存、提交、重新上传均被拒绝，审批通过才允许正常归档。 | prd.md:234 |
| BR-005 | 业务规则，父级 REQ-002 | 表单具有版本概念，审批通过或驳回后再次重新上传会生成+1版本，旧版本变为无效。 | 新版本从空白待提交开始，旧版本不可被当前版本操作误改；旧版本失效动作需实现证据。 | prd.md:243 |
| BR-006 | 业务规则，父级 REQ-002 | 下一步必须上传财务数据 Excel，补充凭证每类不超过 10 个，提交前文件必须与已暂存版本一致。 | 错误提示分别包含请先上传财务表格、不能超过10个、当前文件与已暂存版本不一致。 | prd.md:415 |
| BR-007 | 业务规则，父级 REQ-002 | 2S 店模板既填写售后也填写销售数据，1S 店模板只填写售后数据，基础信息由系统自动带出。 | 模板顺序、门店编码、月份、公式和系统计算字段被严格校验。 | prd.md:435 |
| BR-008 | 业务规则，父级 REQ-002 | 任何一级审批驳回后审批流结束，发起人可以在业务系统重新发起并生成新的审批流。 | 审批流单号不匹配时拒绝；驳回触发 REJECTED，撤回触发 PENDING。 | prd.md:512 |
| REQ-003 | 主需求 | 列表下载字段包含系统计算值，上传审批后的费用数据用于经营模型计算和导出。 | 导出字段、系统计算值、保留两位小数和 BI 同步口径与 PRD 一致。 | prd.md:300 |
| BR-009 | 业务规则，父级 REQ-003 | 导出字段包含财务期间、门店编码、门店名称、门店类型、是否确认以及多类收入成本费用字段。 | 委外成本、总费用、房屋费用小计、单价等字段按模板公式或明细汇总输出。 | prd.md:379 |
| BR-010 | 业务规则，父级 REQ-003 | BI 系统同步时只取每个门店生效中的版本；如果门店无生效中版本，则不取该门店数据。 | 同步数据不得包含非生效版本，业务系统有新生效版本时触发通知。 | prd.md:564 |
| REQ-004 | 主需求 | 总部、区域和门店角色按页面权限、操作权限和数据范围访问财务手工上传功能。 | 总部看全国，区域看区域内门店，门店角色看所属门店。 | prd.md:596 |
| BR-011 | 业务规则，父级 REQ-004 | 门店财务可查看详情、批量下载、上传、提交审批、撤回审批和重新提交，非财务仅查看下载。 | 无任何权限维度时返回空列表或拒绝访问；门店财务只看到所属门店。 | prd.md:603 |

## 关键语义

| ID | 绑定 | 类别 | 业务语义 | 如何验证 | 来源 |
|---|---|---|---|---|---|
| SE-001 | REQ-001, BR-001 | 幂等 | 门店+月份+V1 任务生成必须幂等。 | 调用 createMonthlyExpenseOrders(period)，模拟一个门店已存在、一个门店缺失；断言只插入缺失门店且状态为待提交。 | prd.md:225 |
| SE-002 | BR-002 | 通知 | 开启和催办通知必须按岗位匹配接收人并避免重复发送。 | 调用 sendOpenNotice/sendReminderNotice；断言接收人类型、period、scene 和发送记录状态。 | prd.md:219 |
| SE-003 | BR-004, BR-008 | 状态机 | 表单状态通过 BPM 审批关联控制状态机流转，未注册状态迁移必须拒绝。 | 调用 getStatusEventHandler(origin,target)；断言未注册迁移抛 BusinessException，已注册迁移返回目标处理器。 | prd.md:234 |
| SE-004 | BR-004 | 状态机 | 归档之后不能再人工编辑，归档状态是终态。 | 构造 ARCHIVED 当前版本调用 save/submit/reload；断言 BusinessException 且状态处理器未触发。 | prd.md:236 |
| SE-005 | BR-005 | 版本 | 审批通过或驳回后重新上传生成+1版本，旧版本不能被当前操作误改。 | 对驳回版本调用 reload；断言插入 V2 待提交记录和 V2 快照，旧版本明细不被当作当前版本写入。 | prd.md:243 |
| SE-006 | BR-006, BR-008 | 提交校验 | 提交审批前必须完成 Excel 上传、下一步预览并保证提交文件与暂存一致。 | 分别调用 stepNext/submit；断言缺失 Excel、未下一步和文件不一致均被拒绝，成功路径触发审批中处理器。 | prd.md:415 |
| SE-007 | BR-008 | 跨系统一致性 | BPM 回调必须校验审批流单号，避免旧流程或串单误改当前版本状态。 | 构造 bpmNo 不一致的 approve/reject/cancel；断言 BusinessException 且不触发状态处理器。 | prd.md:507 |
| SE-008 | BR-007, BR-009 | Excel 校验 | Excel 模板顺序、门店编码、月份、公式和系统计算字段必须与标准模板一致。 | 用标准/篡改 Excel 调 parseDetailList；断言标准文件生成明细，篡改顺序或公式时抛 BusinessException。 | prd.md:415 |
| SE-009 | BR-003, BR-009, REQ-004 | 导出 | 列表导出必须按照当前列表展示的数据范围下载 Excel。 | 构造门店权限和明细数据调用 exportList；断言 latestOnly、权限门店列表、导出 URL 和明细查询。 | prd.md:269 |
| SE-010 | REQ-004, BR-011 | 权限 | 总部、区域、门店角色具有不同数据范围，无任何权限维度时不得返回业务数据。 | 构造空权限调用 list；断言 total=0、list 为空且不查询 gateway count。 | prd.md:615 |
| SE-011 | REQ-002, BR-004, BR-005 | 审计证据 | 上传和状态变化必须保留版本、状态、操作人和附件字段证据。 | 调用 stepNext 或 snapshot gateway；断言 update 和 snapshot 字段包含 version、status、operator 和附件 ID。 | prd.md:415 |
| SE-012 | BR-006 | 边界 | 补充凭证每类不超过 10 个文件。 | 构造某一类凭证 11 个文件调用 save；断言 BusinessException 消息包含凭证类别和不能超过10个。 | prd.md:415 |
| SE-013 | BR-006 | 必填校验 | 上传财务数据 Excel 是必填项，未上传时不能进入下一步或提交。 | 调用 stepNext/submit 时不传 financeExcelFileId；断言抛出 BusinessException 且不更新主表。 | prd.md:415 |
| SE-014 | BR-002 | 通知幂等 | 每月15日提醒未提交门店，通知记录必须支撑重跑幂等过滤。 | 调用 notice record insert/update/select；断言 scene、orgId、receiver 和 sendStatus 字段可持久化与读取。 | prd.md:220 |
| SE-015 | BR-010 | BI 同步 | BI 系统定时同步只取业务系统中每个门店生效中的版本。 | 需要 BI 同步接口或消息实现证据；断言同步查询只返回 effective/latest approved 数据，且新生效事件触发通知。 | prd.md:564 |
| SE-016 | BR-005, BR-010 | 版本有效性 | 新版本审批通过生效后，原版本的状态变为无效。 | 需要旧版本失效状态或同步过滤代码；断言新版本生效后旧版本不再作为有效版本输出。 | prd.md:243 |

## GAP 与 OPEN

| ID | 类型 | 关联项 | 风险/问题 | 决策方或补证据方向 |
|---|---|---|---|---|
| GAP-001 | GAP P0 已豁免 | BR-005, BR-010, SE-015, SE-016 | PRD 明确要求新版本生效后旧版本失效且 BI 只取生效版本，但用户已要求本轮忽略。 | 不阻断本轮；后续若重新纳入范围，再补实现证据。 |
| GAP-002 | GAP P1 | BR-002, SE-002 | 飞书图片/画板媒体下载权限不足，通知流程图只能基于浏览器截图和 PRD 文本解析。 | 需要产品或文档 owner 提供可下载画板或确认浏览器解析的流程语义。 |
| OPEN-001 | OPEN | BR-010, SE-015 | BI 同步由本仓库实现、BI 拉取实现，还是其他服务实现？ | 后端负责人/BI 负责人 |
| OPEN-002 | OPEN | BR-005, SE-016 | 旧版本失效是否需要新增状态，还是仅通过 latest/effective 查询口径排除？ | 后端负责人/产品负责人 |

## 自检结论

| 检查项 | 结论 |
|---|---|
| 来源证据 | 每条 REQ/BR/SE 保留 `source` 文件和行号。 |
| 下游可用性 | Q05a 只能使用本报告中的 REQ/BR/SE 作为 EUT 绑定来源。 |
| 不伪造闭环 | `GAP-001` 继续向 Q05a/Q06 传递，不能通过写测试掩盖。 |
