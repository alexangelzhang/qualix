# Q05b 语义与覆盖率联合计划

当前状态：BLOCKED。

## 关键批次

- `blocked-write-permission`：EUT-009 / EUT-022，覆盖索赔/预授权展示链路的小数数量透传。
- 计划断言：`partNumberDecimal=1.50`、`hourInfo.numDecimal=1.50`、`partInfo.numDecimal=1.50`。

## 覆盖率说明

manifest 未提供 JaCoCo `inputs.coverage_report`，`coverage_plan.py` 已阻断，不能生成真实覆盖率 checkpoint 或自报覆盖率。
