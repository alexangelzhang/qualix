# Q01 图片语义记录

本次 PRD 通过 `larkkit download` 下载了 66 个图片资源，目录为 `input/prd_source/static`。Q01 的主规则证据来自 PRD 表格文字与评论摘要，图片主要承担页面位置和入口辅助证据。

## 已识别图片语义

- `prd.md:139` 的图片对应报价单、到店工单、移动工单添加服务项环节，用于支撑“小数配件编辑”发生在添加服务项入口。
- `prd.md:140` 的图片对应工单/财务管理退款入口，用于支撑退款数量规则的页面位置。
- `prd.md:141` 的图片对应预授权入口，用于支撑预授权纳入小数配件编辑范围。
- `prd.md:143` 和 `prd.md:144` 的图片对应售后工作台服务行动、草稿箱、服务项组套入口，用于支撑后台入口范围。
- `prd.md:151` 到 `prd.md:156` 的图片对应报价单、工单、退款、索赔/预授权、服务行动、车辆档案的小数配件展示位置。
- `prd.md:157` 的图片对应押金特殊配件维修和提前交车相关展示，但该范围在 PRD 中标注延期至二期。

## 风险说明

图片未逐张做 OCR 字段级提取；因此展示字段清单已作为 `GAP-001` 保留，后续需要结合原型图或代码确认每个页面的具体字段。
