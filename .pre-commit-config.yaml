repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.9.10
    hooks:
      - id: ruff
        args: [--fix, --exit-non-zero-on-fix, --config, qualix/pyproject.toml]
        files: ^
      - id: ruff-format
        args: [--config, qualix/pyproject.toml]
        files: ^

  - repo: local
    hooks:
      - id: check-arch
        name: Architecture rules (CLAUDE.md)
        entry: python qualix/scripts/check_arch.py
        language: python
        pass_filenames: true
        types: [python]
        files: ^src/dqg/

      - id: schema-prompt-sync
        name: Schema vs SKILL required fields (Q03/Q06)
        entry: python qualix/scripts/check_schema_prompt_sync.py
        language: system
        pass_filenames: false
        files: ^(skills/.*/SKILL\.md|src/qualix/schemas/phase_(q03|q06)\.py)$

      - id: harness-regression
        name: Harness regression guard
        entry: python qualix/scripts/check_harness_regression.py
        language: python
        pass_filenames: true
        files: ^(skills/|src/qualix/quality/|src/dqg/core/phase_registry\.py)
