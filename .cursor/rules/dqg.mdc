---
description: DQG 研发质量门禁框架项目规则
globs: ["**/*"]
---

# dev-quality-gate (DQG) — Cursor 适配层

> 通用项目知识见 `AGENTS.md`（Phase 流程、代码规范、架构、Agent 角色等）。
> 本文件只放 Cursor 特有的规则提示。

## 快速入口

- 全流程引导: 读取 `dqg_starter.md` 开始
- CLI 启动: `python -m dqg.core.cli run <project_id> startup`
- 全量检查: `ruff check src/ tests/ && pytest tests/ -q`

## Skill 文件结构（agentskills.io 标准）

- 所有 Phase skill 在 `skills/<name>/SKILL.md` + `references/` 目录
- SKILL.md < 500 行，详细规则在 references/ 按需加载

## Cursor 特有提示

- 代码补全时遵循 `AGENTS.md` 中的代码铁律（常量管理/JSON操作/异常处理/日志/模块组织）
- 新建文件时参考 `AGENTS.md` 的包结构表，放入正确的子包
- 修改 Phase 相关代码时参考 `AGENTS.md` 的 Common Tasks → Files 表
- 路径映射使用 `constants.py` 的 `PHASE_DIR_MAP`/`STRUCTURED_JSON_MAP`，禁止硬编码
- JSON 读写使用 `json_utils.py` 的 `load_json`/`save_json`，禁止裸 `json.loads`/`json.dumps`

## 关键模块

| 模块 | 用途 |
|------|------|
| `runtime/` | 执行引擎（phase_contract, preflight, credential_pool, harness_ablation） |
| `quality/` | 质量评审（verification_bundle, eval_baseline, score_calibration） |
| `agents/` | Multi-Agent（adaptive_loop, two_phase_worker） |
| `context/` | 上下文（context_loader, context_compressor） |
| `tracking/` | Bug 案例 + Skill Evolution |

*最后更新：2026-04-11*
