# Semantic Coverage Benchmark

This directory contains public, synthetic benchmark cases for Qualix semantic coverage.

Start with [cases.md](cases.md). Each case is short enough to review by hand: requirement, plausible weak test, expected finding.

The benchmark is deliberately small. It should grow by adding cases that expose a real class of missed requirement behavior, not by adding volume.

