name: DQG Self Quality Check

on:
  pull_request:
    paths:
      - 'dev-quality-gate/src/**'
      - 'dev-quality-gate/skills/**'
  push:
    branches: [main]
    paths:
      - 'dev-quality-gate/src/**'

jobs:
  regression:
    name: DQG Regression Suite
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install DQG
        run: pip install -e dev-quality-gate/dev-quality-gate --quiet

      - name: Run regression tests
        run: |
          cd dev-quality-gate/dev-quality-gate
          python -m pytest tests/ -x -q --tb=short

  # CI Gate example (requires finalize artifacts to exist)
  # Uncomment when integrating into a project that runs DQG phases:
  #
  # quality-gate:
  #   name: DQG Quality Gate
  #   runs-on: ubuntu-latest
  #   needs: regression
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: ./dev-quality-gate/dev-quality-gate
  #       with:
  #         project-id: your-project
  #         phase: Q06
  #         fail-on: hard
