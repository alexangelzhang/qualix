# 需求评审产出报告（Phase Q01：产品需求阶段）

> 本模板用于产品需求阶段，默认仅输出：`REQ/BR/SE + GAP + OPEN`。
> `UT/EUT` 在后续单测阶段（Phase Q05a）单独输出。

## PROFILE_CONTEXT
> 从 `output/<project>_phaseA/_profile_context.md` 复制，声明本次分析使用的 profile、baseline、风险词典和阈值。

> **格式规范**: 本报告遵循 `references/report-format-spec.md`。每条结论行必须附 `[来源: 文件名:行号]` 标注。

## 0. 输入与范围
- 需求名称：
- PRD 版本：
- 评审日期：
- 参与角色：
- 变更范围（模块/服务）：
- 假设项（ASSUME）：
- 待确认项（OPEN）：

## 1. REQ/BR/SE 清单

### 1.1 关键语义（SE）防漏清单
| SE ID | PRD 原文摘录 | 语义类别 | 是否已映射到 REQ/BR(是/否) | 映射位置（BR 或子条目） | 备注 |
|---|---|---|---|---|---|
| SE-001 |  |  |  |  |  |

语义类别参考（按需使用）：排序/先后、分组聚合、去重口径、匹配冲突、状态迁移、时间窗口、默认或开关行为、降级行为、跨系统口径转换。

### 1.2 REQ/BR 需求清单（按业务模块分组）

**禁止**把所有 REQ/BR 平铺成 `### REQ-001 → #### BR-001` 层级列表（可读性差，REQ 与 BR 归属不清）。

**必须**按业务模块分组：每个模块一个 `####` 小节，小节内一张 REQ+BR 统一表格（REQ 行在顶，BR 行紧随其后，通过"类型"列区分）。模块划分原则和完整模板见 [skills/requirement-structuring/references/output-templates.md §REQ/BR 需求清单](../skills/requirement-structuring/references/output-templates.md)。

模板示例：

```markdown
#### 1.2.1 模块一：<模块名>（如"编辑小数 - 零售通 PAD"）

父 REQ：**REQ-001** <REQ 描述>（P0）— <一句话总结> [来源: plain_text.txt:xxx]

| ID | 类型 | 描述 | 触发条件 | 行为变化 | 验收标准 | 优先级 | 来源 |
|---|---|---|---|---|---|---|---|
| REQ-001 | REQ |  |  |  |  | P0 |  |
| BR-001 | BR |  |  |  |  | P0 |  |
| BR-002 | BR |  |  |  |  | P1 |  |

#### 1.2.2 模块二：<模块名>

父 REQ：**REQ-002**（若与模块一共享同一 REQ，标注"共享"）

| ID | 类型 | 描述 | 触发条件 | 行为变化 | 验收标准 | 优先级 | 来源 |
|---|---|---|---|---|---|---|---|
| BR-003 | BR |  |  |  |  | P0 |  |
```

## 2. GAP（明显缺口）
> 定义：PRD存在实现依赖，但口径缺失，若不补齐会产生歧义或遗漏。

| GAP ID | 关联 REQ/BR/SE | 缺口描述 | 需补充口径（写入PRD） | 责任人 | 计划时间 |
|---|---|---|---|---|---|
| GAP-001 | REQ-001 / BR-001 / SE-001 |  |  |  |  |

## 3. OPEN（待确认项）
> 定义：存在冲突语义或多解路径，必须评审拍板形成唯一口径。

| OPEN ID | 关联 REQ/BR/SE | 待确认问题 | 备选口径 | 决策人 | 截止时间 |
|---|---|---|---|---|---|
| OPEN-001 | REQ-001 / BR-001 / SE-001 |  | A/B |  |  |

## 4. 完整性校验（Phase Q01 Gate）
- [ ] 每条明确需求都已结构化到 `REQ/BR`
- [ ] 每条关键语义都已进入 `SE`
- [ ] 每个冲突点都已进入 `OPEN`
- [ ] 每个口径缺失都已进入 `GAP`
- [ ] 本报告未输出 `UT/EUT`

## 5. 结论
- 评审结论：`通过 / 有条件通过 / 不通过`
- 结论依据：
- 下一步：进入 Phase Q04（技术方案覆盖度审计）或继续补齐 `GAP/OPEN`
